version: '3.8'

services:
  astro:
    build: .
    ports:
      - "4001:4001"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 4001

volumes:
  node_modules:
